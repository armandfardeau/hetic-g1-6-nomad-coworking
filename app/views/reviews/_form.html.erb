<%= form_for([@office, @office.reviews.new]) do |f| %>
    <%= f.hidden_field :office_id, value: @office.id %>
    <div id='user_stars'></div>
    <div class='form-group row-space-1'>
      <%= f.text_area :comment,
                      rows: 3,
                      class: 'form-control'
      %>
    </div>
    <div class='actions'>
      <%= f.submit 'Add review', class: 'btn btn-primary' %>
    </div>
<% end %>

<script>
    $('#user_stars').raty({
        path: '/assets',
        scoreName: 'review[star]',
        score: 1
    });
</script>