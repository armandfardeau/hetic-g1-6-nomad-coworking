<h3>Reviews <%= @room.average_rating %> (<%= @reviews.count %>)</h3>
<div class="container">
  <div>
    <%= nice_display_comments_form(@booked, @has_review) %>
  </div>
  <div>
    <%= render partial: 'reviews/list' %>
  </div>
</div>